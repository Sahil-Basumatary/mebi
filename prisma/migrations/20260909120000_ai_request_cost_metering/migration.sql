ALTER TABLE "AiUsagePeriod"
ADD COLUMN IF NOT EXISTS "advancedUsed" INTEGER NOT NULL DEFAULT 0,
ADD COLUMN IF NOT EXISTS "advancedReserved" INTEGER NOT NULL DEFAULT 0,
ADD COLUMN IF NOT EXISTS "costMicrosUsed" INTEGER NOT NULL DEFAULT 0;

ALTER TABLE "AiUsageEvent"
ADD COLUMN IF NOT EXISTS "costMicros" INTEGER;
